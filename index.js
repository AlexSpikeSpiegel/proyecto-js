const fn = () => {
    const miString = "soy un string!"
    let b = miString + " muy feliz"
    console.log(b)
}

fn()

const clickeado = () => {
    let a = "estudiambre feliz"

    console.log(a)
}